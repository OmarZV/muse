- @posts.each do |post|
  = link_to (image_tag post.image.url(:small)), post
  %h2= link_to post.title, post
  %p
    = post.comments.count
    Comments

= link_to "Add New Inspiring Training", new_post_path(@post)